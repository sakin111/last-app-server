enum Role {
  USER
  ADMIN
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum TravelType {
  SOLO
  GROUP
  FRIENDS
}

enum PlanType {
  MONTHLY
  YEARLY
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum RequestStatus {
  PENDING
  ACCEPTED
  REJECTED
}
