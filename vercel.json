{
  "version": 3,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node",
      "config": { "runtime": "nodejs22.x" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/src/server.ts" }
  ]
}
